name: Jenkinsfile Runner

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    name: Run Jenkinsfile with Jenkinsfile Runner
    steps:
      - uses: actions/checkout@v2
      - uses: jenkinsci/jenkinsfile-runner-github-actions@770d33f3283f6ce79a3d869e087fe43885f96842
